// import { Checkbox } from "../ui/checkbox";
// import { Label } from "../ui/label";

// type CheckboxProps = { name?: string; lable?: string; defaultValue?: string };
// function Formcheck({ name, label, defaultValue }: CheckboxProps) {
//   const defaultCheckbox = (defaultValue = "on" ? true : false);
//   return (
//     <div className="flex items-center justify-between self-end">
//       <Label htmlFor={name}>{label || name}</Label>
//       <Checkbox name={name} id={name} defaultChecked={defaultCheckbox} />
//     </div>
//   );
// }

// export default Formcheck;
